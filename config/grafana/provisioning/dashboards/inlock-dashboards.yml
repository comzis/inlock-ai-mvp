apiVersion: 1

providers:
  - name: "default"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
      foldersFromFilesStructure: true
  - name: "inlock-observability"
    orgId: 1
    folder: "Inlock Observability"
    folderUid: "inlock-observability"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards/observability
  - name: "devops-platform"
    orgId: 1
    folder: "DevOps Platform"
    folderUid: "devops-platform"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards/devops
  - name: "postgres-overview"
    orgId: 1
    folder: "PostgreSQL"
    folderUid: "postgres-overview"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards/postgres
