# Start Coolify if not running (auto-recovery)
# Runs every 15 minutes. Install: sudo install -m 644 /home/comzis/inlock/ops/security/cron.start-coolify-if-down /etc/cron.d/start-coolify-if-down
# MAILTO sends cron errors to this address instead of root (avoids mail loop on mail.inlock.ai)
MAILTO=milorad.stevanovic@inlock.ai
*/15 * * * * root /home/comzis/inlock/ops/security/start-coolify-if-down.sh
