# Domain and contact
DOMAIN=inlock.ai
EMAIL=admin@inlock.ai

# Traefik / ACME (Cloudflare DNS)
CLOUDFLARE_API_TOKEN=replace-me

# n8n database
N8N_DB=n8n
N8N_DB_USER=n8n
# Password provided via secret file n8n-db-password

# n8n app
N8N_ENCRYPTION_KEY=replace-with-strong-key

# Postgres (shared with n8n)
POSTGRES_DB=${N8N_DB}
POSTGRES_USER=${N8N_DB_USER}
# Password via secret file n8n-db-password
