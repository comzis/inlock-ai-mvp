# Domain and contact
DOMAIN=inlock.ai
EMAIL=admin@inlock.ai

# Traefik / ACME (Cloudflare DNS) – use a scoped DNS-edit token
CLOUDFLARE_API_TOKEN=replace-with-cf-dns-token
# Zone ID for inlock.ai (for DNS/firewall API). Dashboard → inlock.ai → Overview → Zone ID
CLOUDFLARE_ZONE_ID=

# Auth0 Configuration (for OAuth2-Proxy and NextAuth.js)
# Get these from Auth0 Dashboard: https://manage.auth0.com
AUTH0_ISSUER=https://your-tenant.auth0.com
AUTH0_ADMIN_CLIENT_ID=your-admin-client-id
AUTH0_ADMIN_CLIENT_SECRET=your-admin-client-secret

# Auth0 Management API (for automation)
# Create a Machine-to-Machine application in Auth0 Dashboard
# See docs/AUTH0-MANAGEMENT-API-SETUP.md for setup instructions
AUTH0_MGMT_CLIENT_ID=your-management-api-client-id
AUTH0_MGMT_CLIENT_SECRET=your-management-api-client-secret

# OAuth2-Proxy
OAUTH2_COOKIE_SECRET=$(openssl rand -base64 32 | head -c 32)

# Inlock application database (used by inlock-ai service)
INLOCK_DB_USER=inlock
INLOCK_DB_NAME=inlock
# Prefer secrets file; if set here use a strong password (placeholder below)
INLOCK_DB_PASSWORD=change-me-strong-password

# Vault (Dev Mode - DO NOT USE IN PRODUCTION)
# For production, use AppRole authentication instead
VAULT_ROOT_TOKEN=dev-only-token-change-in-production
VAULT_ADDR=http://localhost:8200

# Vault AppRole (Production - Optional)
# VAULT_ROLE_ID=your-role-id
# VAULT_SECRET_ID=your-secret-id

# n8n database
N8N_DB=n8n
N8N_DB_USER=n8n
# Password provided via secret file n8n-db-password

# Postgres (shared with n8n)
POSTGRES_DB=${N8N_DB}
POSTGRES_USER=${N8N_DB_USER}
# Password via secret file n8n-db-password



# =============================================================================
# MAILCOW EMAIL SERVER
# =============================================================================

# Mailcow is deployed separately at /home/comzis/mailcow
# See docs/deployment/MAILCOW-DEPLOYMENT.md for configuration
# Mailcow uses its own mailcow.conf file for configuration
