# Alignment test env for scripts/mta_sts_daily_check.sh
# Copy to mta_sts_align_test.env, fill in secrets, then: source mta_sts_align_test.env && ./scripts/mta_sts_daily_check.sh

ALIGN_TEST_ENABLE=1

# Send test mail FROM this address (use a mailbox that can send via mail.inlock.ai)
ALIGN_TEST_FROM=mta-sts-monitor@inlock.ai
# Or: ALIGN_TEST_FROM=contact@inlock.ai

# Send TO an external mailbox (Gmail, Outlook, etc.) — use a real address (e.g. your Gmail).
# If you use a placeholder or an @inlock.ai address, you'll get "mail loops back to myself".
ALIGN_TEST_TO=your-external@gmail.com

# SMTP: send via mail.inlock.ai (defaults in script: host=MX_HOST, port=587)
ALIGN_TEST_SMTP_HOST=mail.inlock.ai
ALIGN_TEST_SMTP_PORT=587
ALIGN_TEST_SMTP_USER=contact@inlock.ai
ALIGN_TEST_SMTP_PASS=your-contact-mailbox-password

# IMAP: mailbox that receives the test (same as ALIGN_TEST_TO) — use real address and app password
ALIGN_TEST_IMAP_HOST=imap.gmail.com
ALIGN_TEST_IMAP_USER=your-external@gmail.com
ALIGN_TEST_IMAP_PASS=your-gmail-app-password

# Optional: seconds to wait for delivery before fetching (default 15)
# ALIGN_TEST_WAIT=15
